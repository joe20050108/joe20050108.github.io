function displayWarning(text) {
    console.log("%c [Warning] " + text, yellowText);
}

function displayOK(text) {
    console.log("%c [OK] " + text, greenText);
}

function displayError(text) {
    console.log("%c [Error] " + text, redText);
}